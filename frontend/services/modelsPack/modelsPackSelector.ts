import { RootState } from '../store'

export const modelsPacksShortSelector = (state: RootState) => state.modelsPack.modelsPacksShort
export const modelFullSelector = (state: RootState) => state.modelsPack.modelFull
